# Onboarding
Welcome to the team. Your first task is to set up the development environment.

# Deployment
Deployments run through the staging pipeline before production rollout.

# Security
Rotate the database credentials every ninety days and never commit secrets.
