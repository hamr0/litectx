# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts

#### Identity, scope & doctrine
- litectx = pure library (no LLM, no loop, no server). Orchestration → bareagent; budget/trust → bareguard; graph/memory/CE verbs → litectx. baresuite consumes litectx, never the reverse.
- litectx owns all four CE primitives (Write/Select/Compress/Isolate) — the long-running CE library; baresuite = lightweight one-shot automation.
- Source is pure ESM JS + JSDoc. TypeScript is dev-only (tsc checks + generates `.d.ts`; never hand-edit `.d.ts`). CLAUDE.md's "Vitest" reference is stale — the real test runner is `node --test` + `node:assert/strict`.
- NO LSP, ever. Edge resolution = ripgrep `-w` + tree-sitter only. Over-counting is a deliberate design choice (the output is a risk bucket, not a precise reference list).
- Storage = better-sqlite3 + FTS5, single file. Closed question, no alternative store.
- Indexing routed by file extension, never content-sniffed. v1 langs: TS/JS/Python.
- `kind` discriminator first-class; v1 = `code` + `doc`(md). `fact`/`episode`/pdf/docx reserved in schema, deferred.
- Aurora is the port source AND a second opinion only (not an oracle): read its source, carry calibration with file:line provenance, correct its mistakes, never port verbatim. Divergence = a question to investigate, not a bug to fix toward aurora.
- Moat = determinism: no LLM-per-write, no graph DB, no server, single-file SQLite, code-aware graph. Competitors (Mem0/Zep/Cognee/GraphRAG) pay LLM-per-write and/or mandate a graph DB.
- CEDE = the English verb "cede" (hand off to the harness), not an acronym. When CLAUDE.md disagrees with AGENT_RULES.md / LIBRARY_CONVENTIONS.md, those win.
- A deferral must name the exact condition that un-defers it; "later" is insufficient.

#### Recall & ranking
- Recall is kind-scoped: one FTS query per kind, BM25 within the kind, no cross-kind shared ranking (so code-over-md burial is structurally impossible). `recall(q,{kind:"code"})` → flat `Hit[]`; `recall(q)` or array kind → `Record<kind,Hit[]>`.
- v1 default ranking = BM25 + import-spreading, additive (`own + w·spread`, w=0.3 — the only weight positive on all four test repos; 0.7 overfits). Convex fusion rejected (collateral dilution).
- Spreading uses import edges only; call edges don't help recall (they feed impact only).
- Git metadata is display-only grounding, never scored (edit frequency ≠ access frequency).
- Porter stemming rejected for code/doc FTS (breaks recall floors). Two FTS tables: `docs` (unstemmed code+doc) + `mem` (porter-stemmed fact/episode). Activation re-ranks, never gates.
- Code recall is BM25-gated: cosine re-ranks the FTS pool but never nominates for `code` (KNN-union is fact/episode only) — embeddings can't rescue a lexical-gate miss on code.
- `recall()` / `impact()` / `assemble()` are async.

#### Impact
- Invariant: over-count is SAFE, under-count is DANGEROUS. "isolated/low-risk" is a load-bearing safety claim, always hedged; dead code = "review candidate," never "safe to delete."
- `refCount = max(tree-sitter-confirmed, rg -w)`; thresholds ≤2 low / 3–10 med / 11+ high. Unresolved refs recorded as `unresolved`, never `absent`.
- `impact()` needs `rg` on PATH or a symbol falsely reads 0 callers (recall/index don't need it).
- Barrel/alias mitigation (tsalias.js) is impact-only; recall's import resolution stays frozen.

#### Memory & access-log
- Written rows separated from file-indexed rows via a `source` column (`file|direct`); `index()` deletes only `file_index` keys, so written memory survives every index pass. `forget` is scoped to `source='direct'`.
- Access = retrieval-that-was-USED, not appearance in results (appearance-boost = forbidden rich-get-richer). Bind access to ACTIONS: code = edit/content-hash change; facts = write-backs.
- Three signals that ship AT ZERO for ranking (all repo-dependent / falsified — don't re-propose): git-seeded base-level activation, edit→recall re-ranking, trust/stability tie-break. Trust ships as surfaced columns (`provenance`/`use`/`occurredAt`), never a score.
- Episodes: 30-day rolling prune-on-write window (single knob, no count cap); auto-prune runs before the write so a caller's own write isn't self-evicted. Episode timestamps MUST be relative-to-now (hardcoded absolutes break after 30d). `occurred_at` is constitutive for episodes only.
- `promotionCandidates` threshold = 10 (episodes); `reviewCandidates` threshold = 5 (facts). litectx FLAGS for promotion/review; it never distils/summarises (no LLM inside).
- `by`/provenance = WHO asserted (human|agent); `source` = HOW it entered (file|direct) — distinct axes.
- `owner`+`session` scope on fact/episode only (sibling table). Session isolation is load-bearing even solo (recency is not a ranking term). Unset reader (`:me IS NULL`) sees everything (single-tenant default). Never key memory on worktree path (worktree is the harness's concern).
- `force:true` in `index()` must call `clearIndexed()` (file-sourced only), never `reset()` (reset destroys written memory — reserved for the ≤0.1.0 self-heal path).

#### CE verbs (Compress / Isolate)
- API-only by design: stash/peek/evict/assemble (code chooses these). MCP/model verbs: recall/remember/impact/get/index/forget/recent/promotions. `stash` table is plain SQLite (not FTS5) — unindexable, never pruned, reached by exact id only.
- `evict` = stash-only; `forget` = memory-only (structurally separate).
- `compress()` honest savings ≈ 82% bytes WITH the doc kept (never "95–98%"). `signatureOf()` must use tree-sitter body-field extraction, not a line-slice.
- `assemble` = FIT + a COMPRESS tier that rescues would-be-dropped compressible units as signatures in recency order; COMPRESS only fires on units with a parseable `format` (recall-injected code/doc), not raw transcript units.
- SELECT (proactive auto-inject) is permanently killed (~75% noise, no demand); reactive recall/get/impact on agent request is the honest identity.
- Dropped permanently, do NOT rebuild: R-S8 quality/confidence label (POC-falsified — no usable per-query threshold), R-G5 supersede (covered by forget+remember), SELECT auto-inject, R-W7 recordUseful-as-score.
- `summaryWindow`: only the freshest-unit-over-assemble design works (force-on-full overflows; reserve+re-summarize is unstable).
- `contextgraph`: `observe(ctx)` returns a Proxy (instanceof still holds); `trace:true` is opt-in, zero overhead when unset; ships data + Mermaid + taxonomy (SVG renderers stay in examples).

#### Embeddings
- OFF by default in the library (lean/offline + CI byte-identical); CLI + MCP default ON (`--no-embeddings` opts out).
- Model = all-MiniLM-L6-v2 (~23 MB quantized ONNX). Per-query ≈ 0.7s cached / ~2s first download / ~6ms warm (NOT the mis-borrowed 15–19s torch figure). jina-code swap ruled out permanently.
- `@huggingface/transformers` lives in `peerDependencies` + `peerDependenciesMeta.optional` (NOT `optionalDependencies`, which npm installs by default). The `@xenova` CVE chain is closed by migrating to `@huggingface/transformers` v4.

#### Process, release & environment
- Publish = manual OIDC `gh workflow run publish.yml` (workflow_dispatch, trusted publishing, no token); verify end-state on the registry. Never local `npm publish`, never auto-publish. A README/docs-only change still needs a version bump to reach npmjs.
- `main` branch protection is advisory — pushes warn but succeed (user perms bypass).
- Two parallel clones (`~/PycharmProjects/*` and `~/Documents/PycharmProjects/*`) share working-file state — always stage only the specific files you changed.
- `hamr0/AGENT_RULES.md` is the master/origin — apply additions there first, then propagate out; never overwrite it with a thinner project copy.
- Every litectx doc change must propagate to README + `litectx.context.md` + CHANGELOG + CE-PRD + baresuite-PRD — grep to verify before claiming done.
- `.litectx/` index db is auto-created in every git repo's cwd by the Claude Code SessionStart `warm-index.sh` hook (the library never self-indexes). Cleanup proposed but NOT applied: global gitignore `.litectx/`, gate the hook opt-in, drop `--embeddings`.

#### Validation arc (settled — don't re-litigate)
- In-run code-discovery lift is NARROW: recall helps FINDING, not EXECUTING; strong models don't need it, weak models get a nudge-not-rescue. Bench arc is CLOSED — don't fish for more in-run wins; `realwork-bench` stays unbuilt.
- Un-refuted value lives in exactly two places: (1) cross-session persistence (retrieval is the only option for info not in context/weights) — P@5 70% vs 20% OFF but P@1 0% (near-neighbour clustering wins the top slot) = "a shortlist, not a guaranteed #1 hit"; (2) impact's safety invariant (a correctness property, not a speed metric).
- Error-keyed memory (F7): knowing ≠ executing — agents recall the exact pitfall and still fail it; the only benefit is variance collapse (a stabilizer).

#### User working style & guardrails
- POC-first the riskiest assumption. Prove-don't-assert: the test must be able to FAIL — prefer real uncrafted data over a fixture authored to contain the result; bracket every measurement with an input source not coupled to the hypothesis; never `continue` past hard/null cases without counting what was dropped.
- After a real-data POC, replay that data through the SHIPPED code and reconcile any mismatch as a finding, not a footnote. A green suite ≠ a tested surface — verify the built surface against the doc-claimed surface before claiming done.
- The razor: does this signal measure the thing, or something that merely correlates on lucky days?
- Prefers discussion + prose/evidence on design/architecture forks; reserve multiple-choice for discrete, well-scoped choices. Sequence multi-step plans in true execution order; never offer to skip a known fix.
- README/marketing copy: convey value over tech; keep to the honest A/B story (token-saving belongs to compress/assemble which is benched; relevance belongs to memory — never "proven to save tokens" on the decay engine). Verify rendered GFM (`gh api /markdown`) + URL-check before claiming a README pass is done.

## Episodes

### 2026-06-04 - POC gate cleared; build methodology locked
- POC ran on two repos (aurora Py, gitdone JS): graph spreading generalized on both; git-BLA at flat weight did NOT (hurt gitdone). User insight: that was a half-implementation artifact, not proof the idea is dead.
- Locked: walking skeleton + vertical slices, each working end-to-end before integrating; both-repo bench gate mandatory; aurora = second opinion only.
- Lesson: "works by itself" = observable end-to-end behavior; a prior project's 5500 heavy-TDD unit tests with nothing connected was false coverage (inverted Testing Trophy).

### 2026-06-05 - Kind-scoping replaces weighted re-rank (Slice 3)
- Aurora's weighted re-rank needs ≥2 signals; with only BM25 it degenerates to an md-penalty hack. User reframe: agents know their intent, so kinds should never share a ranking.
- Outcome: recall is kind-scoped; shared cross-kind rankings permanently ruled out.

### 2026-06-05 - Activation falsified before building (Slice 4 Step-0)
- POC of ACT-R base-level activation (git commits as pseudo-accesses) across two repos: no weight ≥ baseline on both; git = edit frequency ≠ access frequency.
- POC-first saved building aurora's "336× killer" per-block blame for a signal with no consumer. Spreading promoted as the v1 ranking win; additive fusion at w=0.3.

### 2026-06-05 - Impact safety contract established (Slice 5a)
- Over-count-safe / under-count-dangerous set as the governing invariant. The bench gate immediately caught a real false-isolation (bare `@decorator` not parsed as a call → 0% confirmed; fixed via decoratorTypes).

### 2026-06-09 - Porter stemming rejected; dual-FTS-table settled (Slice 7b)
- Porter-everywhere regressed code recall on three repos (aurora runs porter only as a gate stage, not the ranker). Solution: a second porter-stemmed `mem` table for fact/episode; `docs` stays unstemmed. Morph recall 0.000 → 0.722, code gates byte-identical.

### 2026-06-10 - Two contract bugs caught by live probe, not the green suite
- Chunk-granular recall returned a 455-line container class over its 41-line method (containers are term-count supersets) — fixed with a containment rule; fixtures were happy-path-shaped and missed nesting.
- Separately, `index({force:true})` silently destroyed all written memory (called `reset()`); fixed via scoped `clearIndexed()`. 97 green tests said done; one contract probe caught total data loss. Lesson: probe the real repo and the documented contracts, not just features.

### 2026-06-11 - Edit→recall and trust tie-break both falsified (access-log tier complete)
- Edit-bind predicts next-edit (AUC 0.75–0.98) but as a recall re-ranker it POLLUTES (topic-blind, floats hot chunks for every query; litectx −0.246 MRR). Trust tie-break buries better-BM25 answers and the signals are kind-disjoint.
- Both ship at zero for ranking; trust ships as surfaced columns. Third "ships-at-zero" finding of the tier. Lesson: "predicts next edit" ≠ "predicts query relevance."

### 2026-06-12 - compress() overclaim trilogy corrected before shipping (v0.7.0)
- Three overclaims caught in one session: crafted-bench bias ("0/2→2/2" was doc-exclusive sentinels; real TS 0/3), query-source bias (doc-derived query +0.248 vs fair name-derived −0.003), and a silent-skip bug (38% of methods dropped, inflating 99%→honest 82%). All corrected in source + docs first. The prove-don't-assert pattern recurred three times in one session.

### 2026-06-12 - Tier-A CE well closed on evidence (v0.8.0)
- R-G5 supersede ruled duplicative; R-S8 confidence label POC-falsified (AUC 0.92 aggregate, no usable per-query threshold); `evict()` shipped as the one survivor. Closing a well on evidence (two dropped, one shipped) is the right outcome — don't manufacture work.

### 2026-06-13 - SELECT killed; assemble FIT shipped with a correction
- Proactive auto-inject POC: 21–25% chunk re-supply, 0% outside the dominant repo — robust negative; SELECT permanently killed. Scope model (`owner`+`session`) designed via research; never keyed on worktree.
- assemble FIT POC passed 8/8 (every anomaly a measurement bug found by re-running); the shipped verb replayed the same data at 3.8% drop vs the POC's 1.8% (atomic-overflow artifact) — shipped is more correct. Replay-real-data-through-shipped-code is now doctrine.

### 2026-06-13 - Rigged gates caught and re-grounded (v0.11.0/v0.12.0)
- Session-scope and compress-middle gates were both rigged (crafted corpus; lost-in-the-middle untested). User caught both; rebuilt on real uncrafted data: session isolation is load-bearing (rank-1 stolen 5/6 without scope); lost-in-the-middle is flat at 41k tokens on Sonnet (so no positional compression). prove-don't-assert promoted from prose to a codified AGENT_RULES checklist; nearly clobbered the hamr0 master with a thinner copy — correct move is add-to-origin-then-propagate.

### 2026-06-14 - Live A/B arc closed + F5 cross-session
- Four in-run A/Bs (assemble, impact, markdown-it Opus/Haiku): litectx lost or tied every time; Haiku sign-flipped (~−24%) but nudge-not-rescue; error-keyed memory = variance collapse only. Retrieval was never the coding bottleneck.
- F5 cross-session POC built adversarially (zero-keyword-overlap audit, near-neighbour decoys): P@5 70% vs 20% OFF, P@1 0%. Two CI-gated benches added (assemble 1/1, summaryWindow 3/3), mutation-verified. Bench arc declared CLOSED.

### 2026-06-17 - Ecosystem doc pass + litectx README rewrite (0.16.2)
- Unified the "bare ecosystem" section across 6 repos (prose rows: Core + Optional reach); restructured the hamr0 profile into two pillars (AI & agents / Privacy-first, products vs primitives); rewrote the litectx README value-first (Two cores + Two ways to use it), tech demoted to "Under the hood," internal-doc links dropped, ASCII hero kept.
- Published 0.16.2 via the OIDC workflow; all 64 README URLs verified 200; a GFM list-spacing regression was caught by rendering via `gh api /markdown` before committing. Lesson: render and URL-check before claiming a README pass is done.

## Antigens

### High Confidence (loaded — applies every session)
- None promoted this run. Friction over the global session logs produced 24 clusters, all single-session — below the 5+-session bar for hot-loading. (The durable behavioral lessons — prove-don't-assert, replay-shipped-vs-POC, verify-before-done — are recorded as Facts under "User working style & guardrails," corroborated across many stashes.)

### Medium Confidence (observing — not loaded)
- None this run.

### Low Confidence (needs more data)
- Validate the artifact is actually live/correct after delivery — a clean exit code or green suite ≠ done (friction: "validate correction / regression"; single session, but corroborated by repeated stash lessons).
- Commit/push granularity is the user's call — don't push or commit on every change unless asked (friction: "don't absolute / absolute push / push commit / commit each"; single session).
