# Dependencies
node_modules/

# Generated type declarations (built on publish from JSDoc — never committed)
/types/

# Build / packaging
*.tgz

# litectx index output
.litectx/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
*.swp

# Agent working state — personal, not shared (memory, stashes, friction, settings).
# `.github/` stays tracked; it is CI, not agent state.
.claude/

# Browser-tool scratch
poc/.barebrowse/
.barebrowse/

# generated example data (regenerate: node poc/graph-substrate-poc.mjs)
examples/graph-view/graph.json
